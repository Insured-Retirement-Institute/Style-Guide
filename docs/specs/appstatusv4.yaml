# Modified by Cursor: generated file — edit docs/specs/src/appstatusv4.yaml instead.
# Regenerate with: npm run bundle:specs
openapi: "3.1.0"
info:
  title: "Annuity Application Status APIs"
  description: "APIs for managing Annuity application data including application status, details, transmission, agents, parties, funding, documents, and more."
  version: "3.1.0"
  license:
    name: "MIT"
    url: "https://github.com/Insured-Retirement-Institute/Pending-Case/blob/main/LICENSE"
paths:
  /v3/application-statuses/searches:
    post:
      operationId: "postApplicationStatusSearch"
      summary: "Search for summary application statuses"
      description: "Retrieve summary application statuses based on search criteria."
      tags:
        - "Application Status"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/finishedCutoffDays"
        - $ref: "#/components/parameters/orderBy"
        - $ref: "#/components/parameters/sortOrder"
        - $ref: "#/components/parameters/start"
        - $ref: "#/components/parameters/limit"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchSummaryApplicationStatusesRequest"
      responses:
        "200":
          description: "Policy application statuses successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SummaryApplicationStatusesPagedResult"
        "401":
          description: "Unauthorized"
  /v3/application-statuses/{policyNumber}:
    get:
      operationId: "getApplicationStatusByPolicyNumber"
      summary: "Retrieve application status"
      description: "Retrieve application status for a policy with complete status details."
      tags:
        - "Application Status"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy application status successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DetailApplicationStatus"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
  /v3/application-statuses/policies:
    get:
      operationId: "getPolicies"
      summary: "Retrieve policies"
      description: "Retrieve all policies."
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/associatedFirmId"
        - $ref: "#/components/parameters/cusip"
        - $ref: "#/components/parameters/producerNumber"
        - $ref: "#/components/parameters/npn"
        - $ref: "#/components/parameters/commissionSplitPercent"
        - $ref: "#/components/parameters/crdNumber"
        - $ref: "#/components/parameters/applicationOriginId"
        - $ref: "#/components/parameters/trackingNumber"
        - $ref: "#/components/parameters/finishedCutoffDays"
        - $ref: "#/components/parameters/orderBy"
        - $ref: "#/components/parameters/sortOrder"
        - $ref: "#/components/parameters/start"
        - $ref: "#/components/parameters/limit"
      responses:
        "200":
          description: "Policies successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PoliciesPagedResult"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
  /v3/application-statuses/policies/{policyNumber}:
    get:
      operationId: "getPolicyByPolicyNumber"
      summary: "Retrieve a policy"
      description: "Retrieve summary application-status of a policy without the details of the statuses of each activity associated with the application"
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Policy"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
  /v3/application-statuses/policies/{policyNumber}/fundings:
    get:
      operationId: "getFundingStatusByPolicyNumber"
      summary: "Retrieve policy funding data"
      description: "Retrieve funding data of a policy."
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy funding successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Funding"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
  /v3/application-statuses/policies/{policyNumber}/document-reviews:
    get:
      operationId: "getDocumentReviewStatusByPolicyNumber"
      summary: "Retrieve policy document review data"
      description: "Retrieve document review data of a policy."
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy document review successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentReview"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
  /v3/application-statuses/policies/{policyNumber}/suitability:
    get:
      operationId: "getSuitabilityInformationByPolicyNumber"
      summary: "Retrieve policy suitability data"
      description: "Retrieve suitability data of a policy."
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy suitability successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Suitability"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
  /v3/application-statuses/policies/{policyNumber}/can-sell-validations:
    get:
      operationId: "getCanSellValidationByPolicyNumber"
      summary: "Retrieve policy can sell validation data"
      description: "Retrieve can sell valiation data of a policy."
      tags:
        - "Policy"
      parameters:
        - $ref: "#/components/parameters/correlationId"
        - $ref: "#/components/parameters/brokerNumber"
        - $ref: "#/components/parameters/policyNumber"
        - $ref: "#/components/parameters/associatedFirmId"
      responses:
        "200":
          description: "Policy can sell validation successfully retrieved"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CanSellValidation"
        "401":
          description: "Unauthorized"
        "403":
          description: "Forbidden"
        "404":
          description: "NotFound"
components:
  parameters:
    correlationId:
      name: "correlationId"
      description: "Optional client-supplied identifier to trace the request."
      in: "header"
      required: false
      schema:
        type: "string"
    brokerNumber:
      name: "brokerNumber"
      description: "Optional custom header identifying the broker number of the call"
      in: "header"
      required: false
      schema:
        type: "string"
    policyNumber:
      name: "policyNumber"
      description: "Policy number"
      in: "path"
      required: true
      schema:
        type: "string"
        minLength: 1
        maxLength: 30
    associatedFirmId:
      name: "associatedFirmId"
      description: "Optional parameter for the associated firm ID used to further identify a firm who is doing business on behalf of multiple distribution firms."
      in: "query"
      required: false
      schema:
        type: "string"
        minLength: 1
        maxLength: 65
    cusip:
      name: "cusip"
      description: "Optional query parameter for the CUSIP"
      in: "query"
      required: false
      schema:
        $ref: "#/components/schemas/cusip"
    producerNumber:
      name: "producerNumber"
      description: "Carrier-assigned unique identifier for the producer."
      in: "query"
      required: false
      schema:
        type: "string"
        maxLength: 30
    npn:
      name: "npn"
      description: "The unique National Producer Number that is assigned to an individual producer at the NAIC level that is used as the standard global identifier across all states."
      in: "query"
      required: false
      schema:
        type: "string"
        maxLength: 10
    commissionSplitPercent:
      name: "commissionSplitPercent"
      description: "Represents the percentage share of total commission allocated to the producer, used when multiple producers are associated with the policy."
      in: "query"
      required: false
      schema:
        type: "number"
        minimum: 0
        maximum: 100
    crdNumber:
      name: "crdNumber"
      description: "Optional query parameter for the Central Registration Depository number"
      in: "query"
      required: false
      schema:
        type: "string"
        maxLength: 10
    applicationOriginId:
      name: "applicationOriginId"
      description: "Optional query parameter for the application origin ID"
      in: "query"
      required: false
      schema:
        type: "string"
    trackingNumber:
      name: "trackingNumber"
      description: "Optional query parameter for the delivery tracking number"
      in: "query"
      required: false
      schema:
        type: "string"
    finishedCutoffDays:
      name: "finishedCutoffDays"
      description: "Optional query parameter for when a completed or cancelled policy is to drop off"
      in: "query"
      required: false
      schema:
        type: "integer"
        default: 7
    orderBy:
      name: "orderBy"
      description: "Order by for pagination"
      in: "query"
      required: false
      schema:
        type: "string"
        enum:
          - "policyNumber"
          - "status"
          - "statusDate"
        default: "statusDate"
    sortOrder:
      name: "sortOrder"
      description: "Sort order for pagination"
      in: "query"
      required: false
      schema:
        type: "string"
        enum:
          - "asc"
          - "desc"
        default: "desc"
    start:
      name: "start"
      description: "Starting item for pagination"
      in: "query"
      required: false
      schema:
        type: "integer"
        default: 1
    limit:
      name: "limit"
      description: "Number of items returned"
      in: "query"
      required: false
      schema:
        type: "integer"
        default: 100
        minimum: 1
        maximum: 1000
  schemas:
    cusip:
      description: "CUSIP"
      type: "string"
      minLength: 9
      maxLength: 9
      pattern: "^[0-9]{3}[A-Z0-9]{5}[0-9]$"
    IndividualProducer:
      description: "Schema for individual producer data"
      type: "object"
      required:
        - "relationships"
      allOf:
        - $ref: "#/components/schemas/IndividualParty/allOf/0"
        - type: "object"
          properties:
            npn:
              description: "The unique National Producer Number that is assigned to an individual producer at the NAIC level that is used as the standard global identifier across all states."
              type: "string"
              maxLength: 10
            commissionSplitPercent:
              description: "Represents the percentage share of total commission allocated to the producer, used when multiple producers are associated with the policy."
              type: "number"
              minimum: 0
              maximum: 100
            crdNumber:
              description: "The Central Registiration Depository (CRD) number assigned by FINRA to identify the financial professional."
              type: "string"
              maxLength: 10
            relationships:
              description: "primaryAgent, secondaryAgent, additionAgent, investment Advisor Representative"
              type: "array"
              minItems: 1
              maxItems: 30
              items:
                type: "string"
                enum:
                  - "primaryAgent"
                  - "secondaryAgent"
                  - "additionalAgent"
                  - "investmentAdvisorRepresentative"
    IndividualParty:
      description: "Schema for individual party data"
      type: "object"
      allOf:
        - description: "Schema for individual identity data"
          required:
            - "firstName"
            - "lastName"
          type: "object"
          properties:
            type:
              description: "Indicates whether the party is an individual or an entity."
              type: "string"
              const: "individual"
            firstName:
              description: "First name of the individual"
              type: "string"
              minLength: 1
              maxLength: 105
              pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,105}$"
            middleName:
              description: "The middle name of the individual"
              type: "string"
              minLength: 1
              maxLength: 105
              pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,105}$"
            lastName:
              description: "The last name of the individual"
              type: "string"
              minLength: 1
              maxLength: 105
              pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,105}$"
            taxId:
              description: "Tax Identification Number for the individual or entity"
              type: "string"
              maxLength: 9
        - $ref: "#/components/schemas/PartyRelationship"
    EntityIdentity:
      description: "Schema for entity identity data"
      type: "object"
      required:
        - "type"
        - "name"
      properties:
        type:
          description: "Indicates whether the party is an individual or an entity."
          type: "string"
          const: "entity"
        name:
          description: "Name of the entity"
          type: "string"
          minLength: 1
          maxLength: 180
          pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,180}$"
        taxId:
          description: "Tax Identification Number for the individual or entity"
          type: "string"
    EntityProducer:
      description: "Schema for entity producer data"
      type: "object"
      required:
        - "relationships"
      allOf:
        - $ref: "#/components/schemas/EntityIdentity"
        - type: "object"
          properties:
            npn:
              description: "The unique National Producer Number that is assigned to an individual producer at the NAIC level that is used as the standard global identifier across all states."
              type: "string"
              maxLength: 10
            commissionSplitPercent:
              description: "Represents the percentage share of total commission allocated to the producer, used when multiple producers are associated with the policy."
              type: "number"
              minimum: 0
              maximum: 100
            crdNumber:
              description: "The Central Registiration Depository (CRD) number assigned by FINRA to identify the financial professional."
              type: "string"
              maxLength: 10
            relationships:
              description: "primaryAgent, secondaryAgent, additionAgent, investment Advisor Representative"
              type: "array"
              minItems: 1
              maxItems: 25
              items:
                type: "string"
                enum:
                  - "brokerageGeneralAgency"
                  - "registeredInvestmentAdvisoryFirm"
                  - "independentMarketingOrganization"
                  - "brokerDealer"
    EntityParty:
      description: "Schema for entity party data"
      type: "object"
      allOf:
        - $ref: "#/components/schemas/EntityIdentity"
        - $ref: "#/components/schemas/PartyRelationship"
    PartyRelationship:
      description: "Schema for party relationship data"
      type: "object"
      required:
        - "relationships"
      properties:
        relationships:
          description: "primaryAgent, secondaryAgent, additionAgent, investment Advisor Representative"
          type: "array"
          minItems: 1
          maxItems: 2
          items:
            type: "string"
            enum:
              - "owner"
              - "jointOwner"
              - "annuitant"
              - "jointAnnuitant"
              - "primaryBeneficiary"
              - "contingentBeneficiary"
    Producers:
      description: "Schema for producers data"
      type: "array"
      items:
        anyOf:
          - $ref: "#/components/schemas/IndividualProducer"
          - $ref: "#/components/schemas/EntityProducer"
    Parties:
      description: "Schema for parties data"
      type: "array"
      minItems: 1
      items:
        anyOf:
          - $ref: "#/components/schemas/IndividualParty"
          - $ref: "#/components/schemas/EntityParty"
    CanSellValidation:
      description: "Schema for policy can sell validation data"
      type: "object"
      required:
        - "status"
        - "isActionNeeded"
      properties:
        status:
          description: "Validation status"
          type: "string"
          enum:
            - "awaitingReview"
            - "inProgress"
            - "completed"
        isActionNeeded:
          description: "Indicator that at least one section requires action"
          type: "boolean"
        startDate:
          description: "Start date"
          type: "string"
          format: "date"
        endDate:
          description: "End date"
          type: "string"
          format: "date"
        note:
          description: "Note about the can-sell validation"
          type: "string"
          minLength: 1
          maxLength: 2000
        issues:
          description: "List of issues that need to be addressed"
          type: "array"
          items:
            type: "object"
            required:
              - "type"
            properties:
              type:
                description: "Type of CanSell issue"
                type: "string"
                enum:
                  - "trainingIncompleteMissing"
                  - "trainingExpired"
                  - "agentNotAppointedForSale"
                  - "jitAppointmentInProgress"
                  - "agentNotLicensed"
                  - "other"
              note:
                description: "Note about the issue"
                type: "string"
                minLength: 1
                maxLength: 2000
              affectedProducer:
                description: "Producer for which the CanSell issue exists"
                type: "object"
                properties:
                  npn:
                    description: "The unique National Producer Number that is assigned to an individual producer at the NAIC level that is used as the standard global identifier across all states."
                    type: "string"
                    maxLength: 10
                  commissionSplitPercent:
                    description: "Represents the percentage share of total commission allocated to the producer, used when multiple producers are associated with the policy."
                    type: "number"
                    minimum: 0
                    maximum: 100
                  crdNumber:
                    description: "The Central Registiration Depository (CRD) number assigned by FINRA to identify the financial professional."
                    type: "string"
                    maxLength: 10
                  taxId:
                    description: "Tax Identification Number for the producer"
                    type: "string"
                    maxLength: 9
    Suitability:
      description: "Schema for policy suitability data"
      type: "object"
      required:
        - "status"
        - "isActionNeeded"
      properties:
        status:
          description: "Suitability status"
          type: "string"
          enum:
            - "awaitingReview"
            - "inProgress"
            - "completed"
            - "notApplicable"
        isActionNeeded:
          description: "Indicator that at least one section requires action"
          type: "boolean"
        startDate:
          description: "Start date"
          type: "string"
          format: "date"
        endDate:
          description: "End date"
          type: "string"
          format: "date"
        reason:
          description: "Reason suitability requires action"
          type: "string"
          enum:
            - "moreInformationNeeded"
            - "notSuitableForClient"
            - "other"
        note:
          description: "Note about the suitability review"
          type: "string"
          minLength: 1
          maxLength: 2000
        dueDate:
          description: "Due date"
          type: "string"
          format: "date"
        isApproved:
          description: "If status is completed, this field denotes whether suitability was approved or declined"
          type: "boolean"
    DocumentReview:
      description: "Schema for policy document review data"
      type: "object"
      required:
        - "status"
        - "isActionNeeded"
      properties:
        status:
          description: "Document review status"
          type: "string"
          enum:
            - "awaitingReview"
            - "inProgress"
            - "completed"
        isActionNeeded:
          description: "Indicator that at least one section requires action"
          type: "boolean"
        startDate:
          description: "Start date"
          type: "string"
          format: "date"
        endDate:
          description: "End date"
          type: "string"
          format: "date"
        note:
          description: "Note about the document review"
          type: "string"
          minLength: 1
          maxLength: 2000
        issues:
          description: "List of issues that need to be addressed"
          type: "array"
          items:
            type: "object"
            properties:
              reason:
                description: "Reason the document review issue was raised"
                type: "string"
                enum:
                  - "applicationIssue"
                  - "contactCarrier"
                  - "incompleteForm"
                  - "missingForm"
                  - "missingSignature"
                  - "needClarity"
                  - "other"
              formName:
                description: "Form name"
                type: "string"
                minLength: 1
                maxLength: 200
              formNumber:
                description: "Form number"
                type: "string"
                minLength: 1
                maxLength: 100
              dueDate:
                description: "Due date"
                type: "string"
                format: "date"
              note:
                description: "Note about the document review issue"
                type: "string"
                minLength: 1
                maxLength: 2000
    Funding:
      description: "Schema for policy funding data"
      type: "object"
      required:
        - "isActionNeeded"
        - "status"
      properties:
        startDate:
          description: "Start date"
          type: "string"
          format: "date"
        endDate:
          description: "End date"
          type: "string"
          format: "date"
        isActionNeeded:
          description: "Indicator that at least one section requires action"
          type: "boolean"
        status:
          description: "Funding status"
          type: "string"
          enum:
            - "awaitingReview"
            - "inProgress"
            - "completed"
        note:
          description: "Note about the funding"
          type: "string"
          minLength: 1
          maxLength: 2000
        sources:
          description: "Lists sources of funding"
          type: "array"
          items:
            type: "object"
            required:
              - "method"
              - "type"
            properties:
              method:
                description: "Funding method"
                type: "string"
                enum:
                  - "anotherPolicy"
                  - "check"
                  - "clearinghouse"
                  - "creditCard"
                  - "debitCard"
                  - "digitalPayment"
                  - "electronicFundTransfer"
                  - "payrollDeduction"
                  - "wireTransfer"
              type:
                description: "Type of funding transaction"
                type: "string"
                enum:
                  - "contribution"
                  - "directRollover"
                  - "employeeContribution"
                  - "employerContribution"
                  - "full1035Exchange"
                  - "indirectRollover"
                  - "nonQualifiedTransferOfAssets"
                  - "partial1035Exchange"
                  - "qualifiedTransfer"
              isActionNeeded:
                description: "Indicator that this funding source requires action"
                type: "boolean"
              anticipatedAmount:
                description: "Anticipated amount"
                type: "number"
                format: "float"
              receivedAmount:
                description: "Received amount"
                type: "number"
                format: "float"
              receivedDate:
                description: "Received date"
                type: "string"
                format: "date"
              status:
                description: "Status of the funding source"
                type: "string"
                enum:
                  - "inProgress"
                  - "complete"
              reason:
                description: "Reason the funding is delayed or flagged"
                type: "string"
                enum:
                  - "awaitingFundsFromCedingCompany"
                  - "contactTheCarrier"
                  - "fundingDiscrepancy"
                  - "nigoAtCedingCarrier"
                  - "noFundsReceivedCash"
                  - "pendingReg60"
                  - "other"
              effectiveDate:
                description: "Effective date of policy"
                type: "string"
                format: "date"
              note:
                description: "Note about the funding source"
                type: "string"
                minLength: 1
                maxLength: 2000
              cedingCompany:
                description: "Name of the company holding the existing contract to be replaced."
                type: "object"
                required:
                  - "name"
                  - "accountNumber"
                properties:
                  name:
                    description: "Name of the entity"
                    type: "string"
                    minLength: 1
                    maxLength: 180
                    pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,180}$"
                  accountNumber:
                    description: "Ceding company account number"
                    type: "string"
                  startDate:
                    description: "Start date"
                    type: "string"
                    format: "date"
                  referenceNumber:
                    description: "Reference number"
                    type: "string"
                  nextActivityDate:
                    description: "Next activity date"
                    type: "string"
                    format: "date"
                  note:
                    description: "Note about the ceding company"
                    type: "string"
                    minLength: 1
                    maxLength: 2000
                  methodOfInitiation:
                    description: "Initiation method"
                    type: "string"
                    enum:
                      - "fax"
                      - "email"
                      - "mail"
                      - "replacement"
    PolicySummary:
      description: "Schema for policy summary data"
      type: "object"
      required:
        - "policyNumber"
        - "applicationOrigin"
        - "cusip"
        - "status"
        - "statusDate"
      properties:
        policyNumber:
          description: "Policy number"
          type: "string"
          minLength: 1
          maxLength: 30
        associatedFirmId:
          description: "Optional query parameter for the associated firm ID used to further identify a firm who is doing business on behalf of multiple distribution firms."
          type: "string"
        applicationOrigin:
          description: "Platform of application origin"
          type: "object"
          properties:
            platform:
              description: "Platform of application origin"
              type: "string"
              maxLength: 50
            isElectronic:
              description: "Is application electronic?"
              type: "boolean"
            id:
              description: "ID of application origin"
              type: "string"
              maxLength: 65
        cusip:
          description: "CUSIP"
          $ref: "#/components/schemas/cusip"
        productName:
          description: "Product name"
          type: "string"
          maxLength: 180
        isQualified:
          description: "Is policy tax plan qualified?"
          type: "boolean"
        status:
          description: "Application status"
          type: "string"
          enum:
            - "received"
            - "inProgress"
            - "completed"
            - "cancelled"
        statusDate:
          description: "Date of the application status"
          type: "string"
          format: "date"
        delivery:
          description: "Pertains to delivery of the issued policy/contract"
          type: "object"
          properties:
            sentDate:
              description: "Delivery sent date"
              type: "string"
              format: "date"
            method:
              description: "Delivery method"
              type: "string"
              enum:
                - "fedex"
                - "ups"
                - "electronicDelivery"
                - "handDelivery"
                - "usps"
            trackingNumber:
              description: "Delivery tracking number"
              type: "string"
        caseManager:
          description: "Case manager"
          type: "object"
          required:
            - "fullName"
            - "phoneNumber"
          properties:
            fullName:
              description: "Full name of case manager"
              type: "string"
              minLength: 1
              maxLength: 180
              pattern: "^[a-zA-Z0-9=()*@#\\s.,'_/~%$!();:&\"®`\\-\\\\?\"]{1,180}$"
            phoneNumber:
              description: "Phone number of the party."
              type: "string"
              minLength: 10
              maxLength: 15
            emailAddress:
              description: "Email address of the party."
              type: "string"
            websiteURL:
              description: "Website URL of case manager"
              type: "string"
        producers:
          $ref: "#/components/schemas/Producers"
        parties:
          $ref: "#/components/schemas/Parties"
    Policy:
      description: "Schema for policy data"
      type: "object"
      allOf:
        - $ref: "#/components/schemas/PolicySummary"
      properties:
        isActionNeeded:
          description: "Indicator that at least one section requires action"
          type: "boolean"
        actionSections:
          description: "Required if isActionNeeded is true"
          type: "array"
          items:
            type: "string"
            enum:
              - "suitability"
              - "cansellvalidation"
              - "documentReview"
              - "funding"
        signedDate:
          description: "Signed date of policy"
          type: "string"
          format: "date"
        receivedDate:
          description: "Received date"
          type: "string"
          format: "date"
        issuedDate:
          description: "Issued date of policy"
          type: "string"
          format: "date"
        effectiveDate:
          description: "Effective date of policy"
          type: "string"
          format: "date"
        rateEffectiveDate:
          description: "Rate effective date of policy"
          type: "string"
          format: "date"
        fundsHeldUntilDate:
          description: "Funds held until date"
          type: "string"
          format: "date"
        cancellation:
          description: "Details about the policy cancellation"
          type: "object"
          required:
            - "initiator"
            - "cancelledDate"
          properties:
            initiator:
              description: "Who initiated cancellation"
              type: "string"
              enum:
                - "advisor"
                - "carrier"
                - "client"
            cancelledDate:
              description: "Cancellation date"
              type: "string"
              format: "date"
            reason:
              description: "Reason for the cancellation"
              type: "string"
              minLength: 1
              maxLength: 2000
    Policies:
      description: "Schema for policies"
      type: "array"
      items:
        allOf:
          - $ref: "#/components/schemas/PolicySummary"
          - type: "object"
    PoliciesPagedResult:
      description: "Schema for the paged result of policies"
      type: "object"
      properties:
        startIndex:
          description: "Zero-based index of the first record to return."
          type: "integer"
        itemsCount:
          description: "Number of records included in the current response page."
          type: "integer"
        totalItemsCount:
          description: "Total number of records available that match the request criteria, independent of pagination."
          type: "integer"
          maximum: 1000
        policies:
          $ref: "#/components/schemas/Policies"
    DetailApplicationStatus:
      description: "Schema for detail application status"
      type: "object"
      allOf:
        - type: "object"
          properties:
            correlationId:
              type: "string"
              description: "Optional client-supplied identifier to trace the request."
        - $ref: "#/components/schemas/Policy"
        - type: "object"
          properties:
            canSellValidations:
              $ref: "#/components/schemas/CanSellValidation"
            documentReviews:
              $ref: "#/components/schemas/DocumentReview"
            suitability:
              $ref: "#/components/schemas/Suitability"
            fundings:
              $ref: "#/components/schemas/Funding"
    SummaryApplicationStatuses:
      description: "Schema for application statuses"
      type: "array"
      items:
        $ref: "#/components/schemas/PolicySummary"
    SummaryApplicationStatusesPagedResult:
      description: "Schema for the paged result of application statuses"
      type: "object"
      properties:
        correlationId:
          description: "Optional client-supplied identifier to trace the request."
          type: "string"
        startIndex:
          description: "Zero-based index of the first record to return."
          type: "integer"
        itemsCount:
          description: "Number of records included in the current response page."
          type: "integer"
          maximum: 1000
        totalItemsCount:
          description: "Total number of records available that match the request criteria, independent of pagination."
          type: "integer"
        applicationStatuses:
          $ref: "#/components/schemas/SummaryApplicationStatuses"
    SearchSummaryApplicationStatusesRequest:
      description: "Schema for searching summary application statuses"
      type: "object"
      properties:
        policyNumber:
          description: "Policy number"
          type: "string"
          minLength: 1
          maxLength: 30
        associatedFirmId:
          description: "Optional query parameter for the associated firm ID used to further identify a firm who is doing business on behalf of multiple distribution firms."
          type: "string"
          minLength: 1
          maxLength: 65
        cusip:
          description: "CUSIP"
          $ref: "#/components/schemas/cusip"
        producerNumber:
          description: "Carrier-assigned unique identifier for the producer."
          type: "string"
          maxLength: 30
        npn:
          description: "The unique National Producer Number that is assigned to an individual producer at the NAIC level that is used as the standard global identifier across all states."
          type: "string"
          maxLength: 10
        commissionSplitPercent:
          description: "Represents the percentage share of total commission allocated to the producer, used when multiple producers are associated with the policy."
          type: "number"
          minimum: 0
          maximum: 100
        crdNumber:
          description: "The Central Registiration Depository (CRD) number assigned by FINRA to identify the financial professional."
          type: "string"
          maxLength: 10
        partyTaxId:
          description: "Party’s tax identification number"
          type: "string"
          maxLength: 9
        applicationOriginId:
          description: "Optional query parameter for the application origin ID"
          type: "string"
        trackingNumber:
          description: "Optional query parameter for the delivery tracking number"
          type: "string"
          minLength: 1
          maxLength: 65
        finishedCutoffDays:
          description: "Optional query parameter for when a completed or cancelled policy is to drop off"
          type: "integer"
          default: 7
      additionalProperties: false
