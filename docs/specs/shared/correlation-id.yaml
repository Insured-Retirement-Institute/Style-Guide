# Modified by Cursor: shared correlationId header and request parameter
components:
  headers:
    correlationId:
      description: Correlates this response to downstream processing.
      schema:
        type: string
        maxLength: 100

  parameters:
    CorrelationIdHeader:
      name: correlationId
      description: Optional client-supplied identifier to trace the request.
      in: header
      required: false
      schema:
        type: string
        maxLength: 100
